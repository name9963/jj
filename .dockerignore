node_modules
.runtime
tests
uploads/*.png
uploads/*.jpg
uploads/*.mp4
uploads
.git
# 上一版本地 Python LaMa 方案残留，几 GB，绝不能进镜像
ai-venv
ai-inpaint
*.log

# 模型由 builder 阶段下载后 COPY 进来；本地这份 lama.onnx 有 89MB，
# 若被 `COPY . .` 带进镜像会白白多占一层空间。
models/*.onnx
models/*.bin

# Python 缓存与临时目录
**/__pycache__
*.pyc
_to_delete
test_live.js
